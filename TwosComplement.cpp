/* Twos complement is uesd for negative number system storing in binary.
    if a Binary Number starts (MSB) with 0 then it is a POSITVE NUMBER 
    or if a binary number starts (MSB) with 1 its is a NEGETIVE NUMBER
*/

